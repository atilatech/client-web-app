name: Continuous Integration

on:
  - push

jobs:
  buildAndTest:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: Install dependencies
        run: npm run

      - name: Build
        run: npm run build

      - name: Test
        run: npm run test